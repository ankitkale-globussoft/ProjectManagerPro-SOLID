<Window
    x:Class="ProjectManagerPro_SOLID.Views.AddTaskWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ProjectManagerPro_SOLID.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="clr-namespace:ProjectManagerPro_SOLID.ViewModels"
    Title="AddTaskWindow"
    Width="400"
    Height="Auto"
    d:DataContext="{d:DesignInstance Type=vm:AddTaskViewModel}"
    Icon="/Assets/Images/logo.ico"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer CanContentScroll="True" VerticalScrollBarVisibility="Auto">
            <Grid Margin="10">
                <StackPanel>
                    <TextBlock Text="Title" />
                    <TextBox
                        x:Name="TitleBox"
                        Margin="0,5"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Text="{Binding Title, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock
                        FontSize="12"
                        Foreground="Red"
                        Text="{Binding TitleError}" />

                    <TextBlock Margin="0,10,0,0" Text="Assigned to (Developer)" />
                    <ComboBox
                        x:Name="AssignedToBox"
                        Margin="0,5"
                        FontSize="16"
                        FontWeight="SemiBold"
                        ItemsSource="{Binding Developers}"
                        SelectedItem="{Binding SelectedAssignedTo, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock
                                    FontFamily="Arial Black"
                                    FontSize="14"
                                    FontWeight="DemiBold"
                                    Text="{Binding Username}" />
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>
                    <TextBlock
                        FontSize="12"
                        Foreground="Red"
                        Text="{Binding AssignedToError}" />

                    <TextBlock Margin="0,10,0,0" Text="Priority" />
                    <ComboBox
                        x:Name="PriorityBox"
                        Margin="0,5"
                        FontSize="16"
                        FontWeight="SemiBold"
                        SelectedValue="{Binding SelectedPriority, Mode=TwoWay}"
                        SelectedValuePath="Content">
                        <ComboBoxItem Content="VeryHigh" />
                        <ComboBoxItem Content="High" />
                        <ComboBoxItem Content="Medium" />
                        <ComboBoxItem Content="Least" />
                        <ComboBoxItem Content="VeryLeast" />
                    </ComboBox>
                    <TextBlock
                        FontSize="12"
                        Foreground="Red"
                        Text="{Binding PriorityError}" />

                    <TextBlock Margin="0,10,0,0" Text="Strat Date" />
                    <DatePicker
                        x:Name="StartDatePicker"
                        Margin="0,5"
                        FontSize="16"
                        FontWeight="SemiBold"
                        SelectedDate="{Binding StartDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock
                        FontSize="12"
                        Foreground="Red"
                        Text="{Binding StartDateError}" />

                    <TextBlock Margin="0,10,0,0" Text="Due Date" />
                    <DatePicker
                        x:Name="DueDatePicker"
                        Margin="0,5"
                        FontSize="16"
                        FontWeight="SemiBold"
                        SelectedDate="{Binding DueDate, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBlock
                        FontSize="12"
                        Foreground="Red"
                        Text="{Binding DueDateError}" />

                    <TextBlock Margin="0,10,0,0" Text="Description" />
                    <TextBox
                        Height="100"
                        Margin="0,5"
                        AcceptsReturn="True"
                        FontSize="16"
                        FontWeight="SemiBold"
                        Text="{Binding Description, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                        TextWrapping="Wrap" />
                    <TextBlock
                        FontSize="12"
                        Foreground="Red"
                        Text="{Binding DescriptionError}" />
                </StackPanel>

            </Grid>
        </ScrollViewer>

        <Button
            Grid.Row="1"
            Margin="0,0,0,20"
            Padding="10,0"
            HorizontalAlignment="Center"
            Command="{Binding AddTaskCommand}"
            Content="Add Task"
            Style="{DynamicResource MaterialDesignFlatMidBgButton}" />
    </Grid>
</Window>
