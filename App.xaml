<Application
    x:Class="ProjectManagerPro_SOLID.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="clr-namespace:ProjectManagerPro_SOLID"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    StartupUri="MainWindow.xaml">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!--  Base Light Theme  -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.Light.xaml" />

                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesign2.Defaults.xaml" />

                <!--  Primary color  -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Primary/MaterialDesignColor.Blue.xaml" />

                <!--  Secondary (Accent) color  -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignColors;component/Themes/Recommended/Secondary/MaterialDesignColor.DeepOrange.xaml" />

                <!--  Input control styles  -->
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.TextBox.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.ComboBox.xaml" />
                <ResourceDictionary Source="pack://application:,,,/MaterialDesignThemes.Wpf;component/Themes/MaterialDesignTheme.PasswordBox.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <Style TargetType="Window">
                <Setter Property="Icon" Value="/Assets/Images/logo.ico" />
            </Style>

            <!--  Default Gradient Brush for Buttons  -->
            <LinearGradientBrush x:Key="DefaultButtonGradient" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Offset="0.0" Color="#FF2196F3" />
                <GradientStop Offset="1.0" Color="#FF64B5F6" />
            </LinearGradientBrush>

            <!--  Hover Gradient Brush for Buttons  -->
            <LinearGradientBrush x:Key="HoverButtonGradient" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Offset="0.0" Color="#FF1976D2" />
                <GradientStop Offset="1.0" Color="#FF42A5F5" />
            </LinearGradientBrush>

            <!--  Default Shadow Effect  -->
            <DropShadowEffect
                x:Key="DefaultShadow"
                BlurRadius="8"
                Direction="315"
                Opacity="0.3"
                ShadowDepth="2"
                Color="Black" />

            <!--  Hover Shadow Effect  -->
            <DropShadowEffect
                x:Key="HoverShadow"
                BlurRadius="12"
                Direction="315"
                Opacity="0.4"
                ShadowDepth="4"
                Color="Black" />

            <!--  Button Style  -->
            <Style x:Key="MaterialButtonStyle" TargetType="{x:Type Button}">
                <Setter Property="Padding" Value="12" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="FontWeight" Value="Medium" />
                <Setter Property="Foreground" Value="White" />
                <Setter Property="Background" Value="{StaticResource DefaultButtonGradient}" />
                <Setter Property="BorderThickness" Value="0" />
                <Setter Property="materialDesign:ButtonAssist.CornerRadius" Value="8" />
                <Setter Property="Effect" Value="{StaticResource DefaultShadow}" />
                <Setter Property="Cursor" Value="Hand" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="{x:Type Button}">
                            <Border
                                x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding materialDesign:ButtonAssist.CornerRadius}">
                                <ContentPresenter
                                    Margin="{TemplateBinding Padding}"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center" />
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource HoverButtonGradient}" />
                                    <Setter Property="Effect" Value="{StaticResource HoverShadow}" />
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="{StaticResource HoverButtonGradient}" />
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect
                                                BlurRadius="4"
                                                Direction="315"
                                                Opacity="0.2"
                                                ShadowDepth="1"
                                                Color="Black" />
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter Property="Opacity" Value="0.6" />
                                    <Setter Property="Foreground" Value="#FF999999" />
                                    <Setter Property="Effect">
                                        <Setter.Value>
                                            <DropShadowEffect Opacity="0" ShadowDepth="0" />
                                        </Setter.Value>
                                    </Setter>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <!--  Implicit Style applied to all buttons  -->
            <Style BasedOn="{StaticResource MaterialButtonStyle}" TargetType="{x:Type Button}" />

            <Style x:Key="PriorityBadgeStyle" TargetType="Border">
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="BorderThickness" Value="1" />
                <Setter Property="BorderBrush" Value="{DynamicResource MaterialDesignBody}" />
                <Setter Property="Padding" Value="6,2" />
                <Setter Property="VerticalAlignment" Value="Center" />
                <Setter Property="Margin" Value="8,0,0,0" />
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="SnapsToDevicePixels" Value="True" />

                <Style.Triggers>
                    <DataTrigger Binding="{Binding Priority}" Value="VeryHigh">
                        <Setter Property="Background" Value="#D32F2F" />
                        <!--  Red  -->
                        <Setter Property="BorderBrush" Value="#B71C1C" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Priority}" Value="High">
                        <Setter Property="Background" Value="#F57C00" />
                        <!--  Orange  -->
                        <Setter Property="BorderBrush" Value="#E65100" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Priority}" Value="Medium">
                        <Setter Property="Background" Value="#FBC02D" />
                        <!--  Yellow  -->
                        <Setter Property="BorderBrush" Value="#F9A825" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Priority}" Value="Least">
                        <Setter Property="Background" Value="#388E3C" />
                        <!--  Green  -->
                        <Setter Property="BorderBrush" Value="#1B5E20" />
                    </DataTrigger>
                    <DataTrigger Binding="{Binding Priority}" Value="VeryLeast">
                        <Setter Property="Background" Value="#1976D2" />
                        <!--  Blue  -->
                        <Setter Property="BorderBrush" Value="#0D47A1" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>